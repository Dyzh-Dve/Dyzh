local res = game:GetService('HttpService'):JSONDecode(game:HttpGet('https://games.roblox.com/v1/games/' .. game.PlaceId .. '/servers/Public?sortOrder=Asc&limit=100'))
for _, server in pairs(res.data) do
	if server.playing < server.maxPlayers then
		game:GetService('TeleportService'):TeleportToPlaceInstance(game.PlaceId, server.id)
		break
	end
end

queueonteleport(([[
  repeat
    task.wait()
  until game:IsLoaded()
  if #workspace.SpecialKeys:GetChildren() == 0 then
    print(1)
    loadstring(game:HttpGet("https://raw.githubusercontent.com/Dyzh-Dve/Dyzh/refs/heads/main/1"))()
  else
    print(2)
  end
]]))
